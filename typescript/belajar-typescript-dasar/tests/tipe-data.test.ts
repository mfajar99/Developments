import { CLIENT_RENEG_WINDOW } from "tls";

describe('Data Type', function () {
   it('should must declare', function () {
      const name: string = "Fajar";
      const balance: number = 1000;
      const isVip:boolean = true;

      console.log(name);
      console.log(balance);
      console.log(isVip);
   })
})